---

const { title } = Astro.props
const { img } = Astro.props
---


<article class="uppercase mb-[30px] w-full md:w-[33%] md:mb-[0px]">
    <img class="mx-auto mb-[20px] w-[60%] md:mt-[40px] sm:w-[80%] sm:mb-[30px]" src={title} alt="title">
    <picture>
        <img class="w-full" src={img} alt="Esto puede ser una imagen">
    </picture>
</article>

<style>
    article:nth-child(2){
        transform: translateY(20px);
    }
    @media screen and (max-width: 768px){
        article:nth-child(2){
        transform: translateY(0px);
    }
        
    }
</style>